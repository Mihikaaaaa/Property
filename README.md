# Real Estate Price Prediction - Big Bucks Real Estate
1. pip install -r requirements.txt
2. python train_model.py            (optional - model is already trained)
3. streamlit run streamlit_app.py   (opens http://localhost:8501)
Real data: put data/transactions.csv (locality, property_type, area_sqft, age_years, metro_km, amenities, txn_year, price) before step 2.
